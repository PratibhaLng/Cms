@model Data.Models.Product

@*@{
    ViewData["Title"] = "Upsert";
}*@

<form method="post" asp-action="Upsert" enctype="multipart/form-data">
    <input asp-for="Id" type="hidden" />
    <div class="border p-3 mt-4">
        <div class="row pb-2">

            <h2 class="text-primary">@(Model.Id != 0 ? "Update" : "Create")Product</h2>
            <hr />
        </div>
        <div asp-validation-summary="All"></div>
        <div class="mb-3">
            <label asp-for="Name"></label>
            <input asp-for="Name" class="form-control" />
            <span asp-validation-for="Name" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Description"></label>
            <textarea asp-for="Description" rows="3" class="form-control"></textarea>
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>
        <div class="mb-3">
            <label asp-for="Price"></label>
            <input asp-for="Price" class="form-control" />
            <span asp-validation-for="Price" class="text-danger"></span>
        </div>

       

@*
        <div class="mb-3">
            <label asp-for="SubCategoryId"></label>
            <select asp-for="SubCategoryId" asp-items="ViewBag.CategoryList" class="form-select" />
            <option disabled selected>--Select Category--</option>
            <span asp-validation-for="CategoryId " class="text-danger"></span>
        </div>*@
        
        <<div class="col-lg-4">
            <select class="form-control" required id="SubCategoryList" asp-for="@Model.SubCategoryId" asp-items="ViewBag.SubCategoryList">
                <option value="">--Select Category--</option>
            </select>
        </div>
        </br>
        </br>
        <div class="col-12">
            @if (Model.Id != 0)
            {
                <button type="submit" class="btn btn-primary">Update</button>
            }
            else
            {
                <button type="submit" class="btn btn-primary style="width:150px">Create</button>

            }
        
        <a asp-action="Index" class="btn btn-secondary">Back to Homepage</a>
    </div>
</form>
